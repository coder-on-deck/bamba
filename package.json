{
  "name": "middle",
  "version": "0.0.0",
  "description": "express middlewares like behavior but outside a webapp scenario",
  "devDependencies": {
    "expect.js": "^0.3.1",
    "istanbul": "^0.4.4",
    "mocha": "^2.5.3",
    "npm-run-all": "^2.2.2",
    "standard": "^7.1.2"
  },
  "scripts": {
    "test": "npm-run-all lint mocha:coverage",
    "lint": "standard",
    "lint:watch" : "nodemon --exec 'npm run lint --silent'",
    "mocha:once": "mocha",
    "mocha:coverage": "istanbul cover _mocha"
  },
  "standard" : {
    "globals" : ["it","describe"]
  }
}
